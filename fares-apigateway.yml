zuul:
  routes:
    fares-apigateway:
        path: /api/**
        serviceId: fares-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org:
      springframework:
        security: DEBUG