spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

security:
  user:
    password: password
  oauth2:
    resource:
      userInfoUri: http://localhost:9000/uaa/user

originairports:
  shutdown: LAX

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


logging:
  level:
    org:
      springframework:
        security: DEBUG
